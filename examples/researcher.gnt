agent Researcher {
    systemPrompt: "You are a helpful research assistant. Use the web_fetch tool to find information."
    use web_fetch
    model: "gpt-4o"
    maxSteps: 5
}

let result = Researcher.userPrompt("What is the capital of France?").invoke()
