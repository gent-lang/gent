// String Interpolation Examples
// Syntax: "text {expression} more text"

// Simple variable interpolation
let userName = "Alice"
let greeting = "Hello, {userName}!"

// Number interpolation (auto-converts to string)
let itemCount = 42
let message = "You have {itemCount} items in your cart."

// Multiple interpolations in one string
let firstName = "John"
let lastName = "Doe"
let fullName = "{firstName} {lastName}"

// Escaped braces when you need literal {}
let codeExample = "Use \{curly braces\} for interpolation"

// Interpolation with agent prompts
agent Greeter {
    systemPrompt: "You are a friendly greeter."
    userPrompt: "Please greet {userName} warmly."
    model: "gpt-4o-mini"
}

let result = Greeter.invoke()
